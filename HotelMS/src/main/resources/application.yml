server:
  port: 8080

spring:
  datasource:
    username: postgres
    password: hzkk4567@A
    url: jdbc:postgresql://localhost:5432/amlan
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true